'use babel';

import LanguageArmaAtomMapbuilderView from '../lib/language-arma-atom-mapbuilder-view';

describe('LanguageArmaAtomMapbuilderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
